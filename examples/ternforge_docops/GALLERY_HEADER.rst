Examples
========

These are complete live programs built only on the public ``ternforge_docops`` API.
They use real dependencies, credentials, and external services when the workflow needs them.
Required CI imports and type-checks them but does not run their live workflows.
